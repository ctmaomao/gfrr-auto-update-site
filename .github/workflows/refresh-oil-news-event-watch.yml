name: Refresh Oil News Event Watch

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  workflow_dispatch:
  schedule:
    # Every 6 hours. Keeps the shared Tavily/Brave monthly request budget below free-tier limits.
    - cron: '37 */6 * * *'

concurrency:
  group: gfrr-main-writer-main
  cancel-in-progress: false
  queue: max

permissions:
  contents: write

jobs:
  refresh:
    if: ${{ github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          ref: main
          fetch-depth: 0

      - name: Sync latest main before refresh
        run: git pull --ff-only origin main

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24
          cache: npm
          package-manager-cache: false

      - run: npm ci

      - name: Refresh oil news event watch
        env:
          TAVILY_API_KEYS: ${{ secrets.TAVILY_API_KEYS }}
          BRAVE_API_KEYS: ${{ secrets.BRAVE_API_KEYS }}
        run: npm run build:oil-news-event-watch

      - run: npm run check:oil-news-event-watch

      - name: Upload sanitized Web NGrams article shadow observation
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: gdelt-web-ngrams-article-shadow-${{ github.run_id }}
          path: manual-artifacts/oil-news/gdelt-web-ngrams-article-shadow-latest.json
          if-no-files-found: error
          retention-days: 35

      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/oil-news-event-watch.json data/gdelt-news-cache.json
          if ! git diff --cached --quiet; then
            npm run check:all
            git commit -m "chore: refresh oil news event watch"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push
          fi
