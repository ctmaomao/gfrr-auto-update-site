# 2026-09-18 健康整改执行清单

## Acceptance baseline

Owner 要求先复核四项旧问题，再逐项解决最新整体体检的问题。沿用串行逻辑任务、逐项 commit/push、独立 AI 有界审阅替代人工审阅、CI 后合并授权。保留源权利、单次费用批准、时效和可信证据门槛；不增加实际 AI 或 ACLED 请求。

## 旧问题复核

- 本机 Node 已为 v24.20.0；运行时补丁版本检查已存在。
- Macro Risk 最终 writer 已复验原始输入、完整输出、内容摘要、review 和最终投影；workflow 已记录实际 checkout SHA。核心检查通过。
- Worker 镜像和指定 API 诊断的超时、固定错误摘要已实现；对应八项回归通过。
- 锁文件已为 Playwright 1.63.0，本机 node_modules 仍为 1.61.1；本次 `npm ci --include=dev --ignore-scripts` 修复本机漂移，34 项浏览器验收通过。SheetJS 仍为 0.20.3。无需重复更改依赖声明。

## 顺序与完成条件

1. **搜索额度故障隔离（实施中）**：Macro Risk、Bubble weekly、Oil News 共享单轮密钥暂停规则；402/432/433 后不重复请求该密钥，其他密钥/来源继续，下轮自动探测。固定 Actions 告警和错误分类，保留所有失败 topic，不能把配额故障解释为可信新闻不足的正常跳过。额度恢复仍依赖上游账户；本修复不宣称恢复 Tavily 服务。
2. **发布一致性（待处理）**：检查实际 writer/dispatch 覆盖，补必要触发；维持最新 main 合并发布、无变化不发布、400/32 天额度保护和宽限期判定。
3. **ACLED 自然验收（待核实）**：既有周/月分频已实现；检查真实计划槽和双站回执，不重复下载，不把 dry-run 当成自动生产验收。
4. **控制面精简（待处理）**：归档已完成交接，明确唯一当前状态，退役过期临时恢复入口；保留预算历史、ADR 和 Git refs。
5. **维护性（待处理）**：仅对已识别热点做小范围纯函数提取，以输出等价回归验证，不重写管线。
6. **压力模型证据（待核实）**：确认自动收集与冻结评审口径正常；84 日/40 输入/12 周是实际时间门槛，不以一次检查代替长期验收。

每项回执在本页追加；无法立即完成的外部额度恢复、自然计划槽和长期观察必须明确列为待验，不冒称完成。

## 第一步验证

18 项定向回归通过（含三个实际 collector 的模拟请求）；`check:changed` 触发完整 `check:all`，退出 0。独立 AI 审阅无阻断；未修改 checker 断言、可信新闻、成本或时效门槛。最终 CI/合并状态以 PR 回执为准。本轮没有真实搜索或 DeepSeek 请求。
