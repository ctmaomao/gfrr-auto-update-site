# 2026-09-18 健康整改执行清单

## Acceptance baseline

Owner 要求先复核四项旧问题，再逐项解决最新整体体检的问题。沿用串行逻辑任务、逐项 commit/push、独立 AI 有界审阅替代人工审阅、CI 后合并授权。保留源权利、单次费用批准、时效和可信证据门槛；不增加实际 AI 或 ACLED 请求。

## 旧问题复核

- 本机 Node 已为 v24.20.0；运行时补丁版本检查已存在。
- Macro Risk 最终 writer 已复验原始输入、完整输出、内容摘要、review 和最终投影；workflow 已记录实际 checkout SHA。核心检查通过。
- Worker 镜像和指定 API 诊断的超时、固定错误摘要已实现；对应八项回归通过。
- 锁文件已为 Playwright 1.63.0，本机 node_modules 仍为 1.61.1；本次 `npm ci --include=dev --ignore-scripts` 修复本机漂移，34 项浏览器验收通过。SheetJS 仍为 0.20.3。无需重复更改依赖声明。

## 顺序与完成条件

1. **搜索额度故障隔离（已合并 #403）**：Macro Risk、Bubble weekly、Oil News 共享单轮密钥暂停规则；402/432/433 后不重复请求该密钥，其他密钥/来源继续，下轮自动探测。固定 Actions 告警和错误分类，保留所有失败 topic，不能把配额故障解释为可信新闻不足的正常跳过。额度恢复仍依赖上游账户；本修复不宣称恢复 Tavily 服务。
2. **发布一致性（已合并 #404；双站已验收）**：检查实际 writer/dispatch 覆盖，补必要触发；维持最新 main 合并发布、无变化不发布、400/32 天额度保护和宽限期判定。
3. **ACLED 自然验收（配置已核实；自然槽待验）**：既有周/月分频已实现；检查真实计划槽和双站回执，不重复下载，不把 dry-run 当成自动生产验收。
4. **控制面精简（实施中）**：归档已完成交接，明确唯一当前状态，退役过期临时恢复入口；保留预算历史、ADR 和 Git refs。
5. **维护性（待处理）**：仅对已识别热点做小范围纯函数提取，以输出等价回归验证，不重写管线。
6. **压力模型证据（已核实；观察期继续）**：确认自动收集与冻结评审口径正常；84 日/40 输入/12 周是实际时间门槛，不以一次检查代替长期验收。

每项回执在本页追加；无法立即完成的外部额度恢复、自然计划槽和长期观察必须明确列为待验，不冒称完成。

## 第一步验证

18 项定向回归通过（含三个实际 collector 的模拟请求）；`check:changed` 触发完整 `check:all`，退出 0。独立 AI 审阅无阻断；未修改 checker 断言、可信新闻、成本或时效门槛。最终 CI/合并状态以 PR 回执为准。本轮没有真实搜索或 DeepSeek 请求。

## 第二步验收基线

PR #403 已合并为 `d173bdd8`，精确 head `f3454ca9` 的 CI 35305671956（依赖审计、单元、完整检查、浏览器）通过，独立审阅绑定相同代码 blob。

进一步核对确认 EdgeOne 的三小时任务已包含全部静态数据，不存在完全遗漏的数据发布路径。此前 ODP 差异在 4 小时宽限期内（2026-09-18T03:59:50Z 双站 12 请求，零 delivery errors）。因此不盲目增加全部 writer 的即时触发，避免突破低频/400 次额度契约。

本步修复 Pages 队列事件可能固定旧事件提交的问题：只允许 main，checkout 最新 main 后记录实际 SHA，在完整检查之后复核同 SHA 再构建；不在校验后 pull。保持串行发布、现行触发来源和全部原检查。

同时仅增加 ODP 成功完成后的 EdgeOne 触发，解除确定性能源数据交付对 Macro AI 成功的偶然依赖；三个现行 editorial 触发均保留。三小时兜底、无变化不发布及 400/32 天硬上限不变。检查器精确名单变更和最多每日一次额外计划唤醒见 [ADR-0057](ADR/0057-publication-current-main-and-odp-tail.md)，独立审阅后才合并。

## 后续核实与控制面精简

- PR #404 合并 `f7c5d015`，精确 head `391cb12a` 的 CI 35306682525 全部通过；独立审阅覆盖发布 workflow 与 checker 变更，自动部署另验。
- ACLED 最新 35300766848 为 dry-run；当前分频已合并，但无自然槽回执，不增加真实下载。运行说明修正为周一/三/五并补齐已完成双站回执；ADR-0055/0056 补入索引。
- 当前研究 cohort 与真实 artifact 35278387481 一致；ledger 1 条、distinctInputs=1、benchmarkWeeks=0，eligibleForProduction=false。只读下载/核验 artifact，无新取数；保留全部冻结门槛。
- 控制面按 ADR-0058 移除已过期执行许可/恢复输入/无用摘要计算，旧 ID 和旧计划一律拒绝。保留全部正常预算测试及历史 refs；原有旧许可正例改为零请求拒绝回归，独立审阅后才合并。
- 完整旧 Backlog 原文移至既有历史文档，当前入口保留 P0–P3、规则、授权和真实待验，不将归档视为关闭未完成事项。

### 研究自然回执更新

等待 CI 期间，计划任务 35312632784 于 2026-09-18T05:54Z 自然成功（不是本轮手动触发）。已只读下载并核验同一 frozen cohort：原 ledger 恢复、记录、派生诊断和 artifact 全部成功；现为 2 条记录、2 个不同输入、elapsedDays=1、benchmarkWeeks=0，eligibleForProduction=false。自动连续性已有一次自然回执，长期收集门槛仍未达到。

### 发布验收

Pages 35318657424 与 EdgeOne 35318657470 均成功。2026-09-18T07:21:31Z 对基线 f7c5d015 的 12 个只读请求确认：两站入口、脚本及四份数据哈希全部一致，ODP 差异消失，零 delivery errors。仅新闻来源降级 WARN 仍在，不能将它说成发布故障或声称外部搜索已恢复。未手动运行数据源或 AI。

### 搜索隔离自然回执

Oil News 自然 schedule 35309521056 成功，事件源码 4ece8e20 已包含 #403。实际刷新阶段 05:06:44Z 出现固定 `http_432_plan_limit` 密钥暂停告警；后续任务成功发布降级结果。Commit 步骤另一次同类日志来自回归检查，不当作第二次真实搜索证据。未由本轮额外触发网络采集；Tavily 额度仍未恢复。
